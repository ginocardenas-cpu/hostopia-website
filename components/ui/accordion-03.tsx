/**
 * Accordion-style hero sidebar (title + body + optional image per row).
 * Integrated on product pages via `ProductHeroAccordion`; this file matches the requested component name.
 */
export {
  ProductHeroAccordion as Accordion03,
  type ProductHeroAccordionProps as Accordion03Props,
  type ProductHeroAccordionItem as Accordion03Item,
} from "@/components/ui/product-hero-accordion";
